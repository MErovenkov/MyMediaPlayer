<resources>
    <style name="Theme.MyMediaPlayer" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/black</item>

        <!-- Fragment -->
        <item name="fragment_background">@color/white</item>

        <!-- Text -->
        <item name="text_primary_color">@color/light_black</item>

        <!-- Image -->
        <item name="image_tint">@color/black</item>>

        <!-- Recycler -->
        <item name="line_divider">@color/gray_55</item>

        <!-- Quality dialog -->
        <item name="quality_mark_color">@color/black</item>
    </style>

    <style name="ProgressBarStyle">
        <item name="colorAccent">@color/white</item>
    </style>
</resources>